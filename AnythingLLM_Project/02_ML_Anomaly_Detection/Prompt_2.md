Act as a Lead Machine Learning Engineer. Write a production-ready, fully type-hinted Python application using FastAPI and Scikit-Learn to deploy an Anomaly Detection model for financial transactions. The script must include: 1. A function to simulate and preprocess complex financial data. 2. Training an Isolation Forest model. 3. A REST API endpoint that accepts JSON transaction data and returns a fraud probability score. Ensure the code follows SOLID principles, includes explicit dependency imports, and contains no placeholders. Write the complete, executable application.